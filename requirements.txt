# Convenience mirror of [project.dependencies] in pyproject.toml — pyproject.toml is authoritative.
# Optional extras ([pi], [voice], [dev], etc.) are intentionally not listed here; install via:
#   pip install -e ".[voice]"   # voice input/output deps
#   pip install -e ".[dev]"     # dev + test deps (preferred for local work)
anthropic
opencv-python
pillow
numpy
python-dotenv
# Optional voice input (mic + STT): install with  pip install -e ".[voice]"  -> sounddevice, faster-whisper
