# Runtime dependencies for yalp. Mirrors [project.dependencies] in pyproject.toml.
# For an editable dev install (preferred), use:  pip install -e ".[dev]"
anthropic
opencv-python
pillow
numpy
python-dotenv
# Optional voice input (mic + STT): install with  pip install -e ".[voice]"  -> sounddevice, faster-whisper
